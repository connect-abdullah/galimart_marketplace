/**
 * Marketplace identity. Update display strings here only.
 */
export const MARKET_NAME = "GaliMart";

/** Public path to the brand logo (place file in /public). */
export const LOGO_SRC = "/Logo.png";
/** First part of the wordmark (primary color). */
export const MARKET_LOGO_PRIMARY = "Gali";
/** Second part of the wordmark (accent color). */
export const MARKET_LOGO_ACCENT = "Mart";

export const REGISTRATION_ID = "FA24-BSE-042";

/** Required footer / contact identification string. */
export const SITE_IDENTIFICATION = `${MARKET_NAME}-${REGISTRATION_ID}-(ShoppingMart)`;
